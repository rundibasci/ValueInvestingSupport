# RM2 — Validation

## Verification Commands (as actually run this session)

1. `cd backend && ./mvnw -o test-compile` — clean compile after every incremental change.
2. `cd backend && ./mvnw -o test -Dtest=SectorClassifierTest,SectorMetricServiceTest,ValueScoreServiceTest,ValuationHistoryServiceTest,SeedServiceTest` — the phase's new/extended unit tests in isolation during implementation: **47/47 passed**.
3. `cd backend && ./mvnw -o test -Dtest=SeedServiceTest,RatioSnapshotRepositoryTest,FmpAdapterTest,FundamentalSnapshotRepositoryTest` — repository/adapter round-trip tests (including the new `RatioSnapshotRepositoryTest` `@DataJpaTest`): **18/18 passed**.
4. `cd backend && ./mvnw -o test` — **full backend suite: 534/534 passed, 0 failures, 0 errors**, run twice (once before, once after the mid-session `interestExpense` addition below) — zero regressions in any pre-existing `financial`/`cyclical`/`dividend-paying`/`non-dividend-growth` scoring, valuation-history, or seeding test.
5. `git diff --stat main` — confirmed to touch only `backend/` files (entities, migration, services, config, tests) plus `application.yml` and this spec directory; the pre-existing, unrelated uncommitted `terraform/environments/staging/variables.tf` change from a prior session was left untouched and is not part of this diff.
6. `terraform`/`frontend` commands: **not applicable** — RM2 touches no `frontend/`/`terraform/` file.

## Acceptance Checks — Result

- [x] `SeedTickerService.persistFundamentals` sets `depreciationAndAmortization`/`ebitda` on both ANNUAL and TTM rows (closes the RM1 seed-path gap) — verified by `SeedServiceTest::seedTickers_persistsDepreciationAndEbitdaOnAnnualAndTtmRows`, which captures both saved entities via `ArgumentCaptor` and asserts non-null values on each, not just that `SectorMetricServiceTest` passes in isolation.
- [x] `SectorClassifier.isReit("Utilities")` → `false`; `isReit("REIT - Retail")`/`isReit("Real Estate")` → `true`; `isReitOrUtility`'s existing behavior unchanged — `SectorClassifierTest` (21/21 total, including the 12 new `isReit`-specific cases).
- [x] `V29__ratio_snapshot_reit_metrics.sql` applies cleanly under Hibernate's `create-drop` schema generation (this project's `test` profile disables Flyway itself — see Known Risks) and all 9 new columns (7 on `ratio_snapshot`, 2 on `fundamental_snapshot`) round-trip via `RatioSnapshotRepositoryTest`.
- [x] `SectorMetricService.compute()` is a confirmed no-op (zero repository interactions, `Mockito.verifyNoInteractions`) for a non-REIT security.
- [x] `SectorMetricService.compute()` on a REIT TTM row with a full paired `FundamentalSnapshot`+`PriceQuote` produces FFO/AFFO/P-FFO/P-AFFO/Net-Debt-EBITDA/interest-coverage/AFFO-payout values matching hand-computed expected results for a known input set — `SectorMetricServiceTest::compute_reitSecurity_ttmRow_computesKnownValues`.
- [x] The ANNUAL-row implied-price derivation (`peRatio × eps`) is exercised and asserted — `SectorMetricServiceTest::compute_reitSecurity_annualRow_derivesImpliedPriceFromPeRatioAndEps`.
- [x] Rows with no matching `FundamentalSnapshot`, non-positive `ebitda`, or zero `interestExpense` leave the affected fields `null` — never a fabricated or divide-by-nonsense value (3 dedicated tests).
- [x] `SectorMetricService.compute()` runs before `ValueScoreService.compute()` in `SeedTickerService.seedOne()` — implemented as a direct call ordering fix (Decision 3); `PipelineRunService` needed no separate change (Decision 11, investigated not assumed).
- [x] `ValuationHistoryService.compute()` produces a `P_FFO` band for a REIT fixture with ≥3 years of `priceToFfo`, and correctly resolves to `INSUFFICIENT_DATA` for a non-REIT fixture with **zero code change** to the non-REIT path — `ValuationHistoryServiceTest::compute_reitFixture_producesPFfoBand` / `::compute_nonReitFixture_pFfoIsInsufficientData`.
- [x] `ValueScoreService`'s five REIT-branch pillar methods each pass a dedicated hand-computed test (`compute_reitSecurity_allFivePillarsUseReitMetrics`), plus a dedicated Safety +2 interest-coverage-bonus test (`compute_reitSecurity_interestCoverageBonusCapAt20`) and a non-REIT regression guard confirming `ValuationHistoryService` is never called off the GAAP path (`compute_nonReitSecurity_neverCallsValuationHistoryService`); every pre-existing test in the class passes unmodified in its assertions (only the constructor-signature call site changed).
- [x] `financial`/`cyclical`/`dividend-paying`/`non-dividend-growth` weight-profile keying and every non-REIT pillar formula are byte-for-byte unchanged — confirmed by the untouched pre-existing `ValueScoreServiceTest` tests passing, and by the diff itself containing only additive `if (isReit)` branches.
- [x] `SectorMetricProperties` binds correctly from `application.yml`'s new `scoring.sector-metric.reit.*` block (Spring context loads without error across the full 534-test suite, which includes `@SpringBootTest`-class tests that would fail on a binding error).
- [x] `git diff --stat main` confirmed backend-only (see Verification Commands #5).

## Corrections Made During Implementation (honest account)

Per this project's own "verify against real code/live data, not spec text" discipline (TA4/TA5/RM1 precedent), several things in `plan.md`'s original sketch turned out different once actually checked:

1. **`interestExpense` is confirmed live, not left `INSUFFICIENT_DATA`.** `plan.md`'s original contingency assumed EBITDA interest coverage might ship unavailable, matching RM1's posture toward NOI. Live confirmation against FMP Premium's real `/income-statement` payload for `O`, `PLD`, and `SPG` (via `curl` against the FMP API directly, this session, real API key) found `interestExpense` present and non-null on all three (`O`: 1,142,201,000; `PLD`: 1,002,593,000; `SPG`: 974,836,000). Wired it in fully: `FmpIncomeStatementEntry.interestExpense`, `FundamentalSnapshot.interestExpenseHistory`/`.interestExpense`, `SectorMetricService.interestCoverageEbitda()`, and a new `fundamental_snapshot.interest_expense` column in `V29`.
2. **The Quality pillar's REIT formula reads from `FundamentalSnapshot`, not `RatioSnapshot`.** The first implementation attempt tried computing FFO margin from `RatioSnapshot.getFfoPerShare()` (a TTM per-share figure) against `FundamentalSnapshot`'s ANNUAL revenue — a period mismatch, and `RatioSnapshot` has no revenue field to pair cleanly anyway. Corrected to compute directly from the same ANNUAL `FundamentalSnapshot` row: `(netIncome + D&A) / revenue`, same period throughout, no per-share conversion needed.
3. **`ValueScoreService`'s REIT MoS pillar calls `ValuationHistoryService.compute()` live**, not via a persisted `ValuationBandResult` lookup, once it became clear during implementation that `ValuationHistoryService.compute()` is itself only ever called on-demand elsewhere in this codebase (`MoatController`, `SecurityReviewService`) — there is no ingestion-time job that populates `valuation_band_result`. A lookup-only approach would have found no `"P_FFO"` row at all for a REIT's very first score computation after seeding. `plan.md`'s Group 7.2 sketch assumed a repository lookup; the actual implementation calls `valuationHistoryService.compute(security)` directly and reads the `P_FFO` entry from its returned list.
4. **`PipelineRunService` needed no separate ordering fix**, contrary to `plan.md` Group 5.2's assumption that it might. Investigation (per that same group's own instruction to confirm before assuming) found `PipelineRunService.run()` delegates seeding entirely to `SeedService.seedTickers()` → `SeedTickerService.seedOne()`, so Decision 3's `SeedTickerService` fix already covers `PipelineRunService`'s own (redundant) second `valueScoreService.compute()` call.
5. **`FundamentalSnapshot.capitalExpenditureHistory` is ingested but not read by `SectorMetricService`.** AFFO's recurring-capex figure reuses the existing `maintenanceCapexDepreciationRatio` config value applied to D&A (`OwnerEarningsCalculator.estimateMaintenanceCapex`), per the definition doc's explicit "reuse the existing config value" instruction (§4.2) — not a split of total capex as one reading of the doc's §3 data-gap table could suggest. `capitalExpenditure` is still ingested (mechanical, low-risk, already-confirmed provider field) for potential future cross-check/display use, but is currently unused by any formula.

## Manual Verification — What Was Actually Done

**Full local-stack manual verification (seed `O`/`PLD`/`SPG` via a running app, `GET /score`) was not performed this session — Docker is unavailable in this environment** (`docker info` fails to connect to the daemon), and this project's `local`/`demo` profiles require Postgres and/or Redis via Docker Compose. This is an environment constraint, not a skipped step — consistent with how TA5's own `validation.md` documented states that were "structurally unreachable" in its session and marked them as such rather than silently omitting them.

In place of a full app run, the following **real, live evidence** was gathered directly against the FMP API (`curl`, this session's real `FMP_API_KEY`, network connectivity confirmed available):

1. **[live]** Confirmed `interestExpense` present and populated on `/income-statement` for `O`, `PLD`, `SPG` (see Corrections #1) — this directly resolved an open item, not a simulated check.
2. **[live]** Fetched `O`'s FY2025 income statement, balance sheet, and quote, and hand-computed every RM2 formula against the real figures as an end-to-end plausibility check (not a substitute for the unit tests above, a supplementary sanity check on the formulas' real-world behavior):
   - FFO/share ≈ $3.90, P/FFO ≈ 15.7× — plausible against Realty Income's typically-observed P/FFO range.
   - AFFO/share ≈ $1.99 (using the 0.70 D&A-ratio heuristic) vs. Realty Income's own reported AFFO/share ≈ $4.22 — a real, material gap, documented in `requirements.md` → Compatibility and Risks as a disclosed limitation of reusing the generic owner-earnings heuristic for a net-lease REIT.
   - Net Debt/EBITDA ≈ 9.1× (raw EBITDA) vs. Realty Income's own reported adjusted (EBITDAre) leverage ≈ 5.5× — a real, material gap, likewise documented, and directly explained by RM1's own confirmed finding that gains/losses on real-estate sales are not isolable from FMP's data.
3. **[not run]** `POST /api/v1/admin/seed?tickers=O,PLD,SPG` against a live running backend, and the corresponding `GET /api/v1/securities/{symbol}/score` responses — blocked on Docker availability in this session. The unit/repository test suite (item 2 in Verification Commands) exercises the same code paths with mocked repositories and hand-verified expected values; item 2 above cross-checks the formulas against real external data by hand. Recommend re-running this step in an environment with Docker available before treating the phase as fully field-validated end-to-end.
4. **[not run]** A non-REIT regression seed (e.g. `AAPL`) through a live running app, to visually confirm its `ValueScore`/`ratio_snapshot` row is unchanged — covered instead by the full 534-test regression suite passing with zero changes to any non-REIT test's assertions.

## Merge Gate

1. `./mvnw -o test` — **534/534 passed**, zero regressions.
2. Every Acceptance Check above is checked, with its supporting test or evidence named.
3. `git diff --stat main` confirmed backend-only.
4. Manual verification is real but partial (live FMP field confirmation + hand-computed formula sanity check on `O`; full running-app seed/score verification not performed, Docker unavailable this session) — disclosed honestly above, not silently assumed complete.
5. `specs/roadmap.md` → Phase RM2 marked `*(complete)*` in the same merge.

## Known Risks (carried forward or introduced by this phase)

- **AFFO's D&A × 0.70 recurring-capex heuristic materially understates AFFO for net-lease REITs** (confirmed with real `O` data, ~$1.99 computed vs. ~$4.22 company-reported) — every AFFO-derived figure (AFFO payout ratio, the Dividend pillar, P/AFFO) inherits this understatement. A REIT-specific recurring-capex heuristic (rather than reusing the generic DCF owner-earnings ratio) is a natural next recalibration target, not attempted in this phase to avoid inventing an unvalidated new assumption under time pressure.
- **Net Debt/EBITDA (raw, unadjusted `ebitda`) reads more conservatively than a REIT's own company-reported EBITDAre-based leverage** (confirmed with real `O` data, ~9.1× vs. ~5.5×) — the Safety pillar's first-pass thresholds (`SectorMetricProperties`) may currently under-score a conservatively-levered REIT. Directly caused by RM1's confirmed, unavoidable absence of a gains/losses-on-sale adjustment field in FMP's data — not something RM2 can fix without a different data source.
- **Quality-pillar formula (FFO margin) and Safety/Dividend thresholds remain first-pass calibrations** (`requirements.md` Decisions 6–7), now with two concrete real-data findings (above) motivating a future recalibration pass, rather than a purely theoretical caveat.
- **`SectorMetricService` is seed/pipeline-time-only, not nightly-scheduled** (`requirements.md` Decision 2) — a REIT security's sector metrics go as stale between seeds as its moat/capital-allocation data already does.
- **`ValueScoreService.computeMosScoreReit` triggers a `ValuationHistoryService.compute()` delete-and-reinsert of all five `ValuationBandResult` rows on every REIT score computation** — consistent with that method's existing on-demand-recompute design used elsewhere, but RM2 adds a third caller. Not a correctness issue.
- **Full local-stack manual verification (live seed + score endpoints) was not performed this session** (Docker unavailable) — recommend running the Manual Verification Sequence's not-run items in a Docker-available environment before or shortly after this phase reaches a production-shaped deployment.
- **REIT MoS is a relative-valuation signal (P/FFO percentile), not an intrinsic-value margin of safety** — RM3's security-detail surfacing and RM4's AI-thesis propagation must carry this distinction forward explicitly (`requirements.md` Decision 8).
