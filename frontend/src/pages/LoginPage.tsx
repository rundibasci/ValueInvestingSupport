import { useState } from 'react'
import type { FormEvent } from 'react'
import { Navigate, useLocation, useNavigate } from 'react-router-dom'
import { useAuth } from '../auth/AuthProvider'

export function LoginPage(): JSX.Element {
  const { session, login, message } = useAuth(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState<string | null>(null); const [submitting, setSubmitting] = useState(false); const location = useLocation(); const navigate = useNavigate()
  const from = (location.state as { from?: { pathname?: string; search?: string; hash?: string } } | null)?.from; const destination = from?.pathname ? `${from.pathname}${from.search ?? ''}${from.hash ?? ''}` : '/'
  if (session) return <Navigate to={destination} replace />
  async function submit(event: FormEvent<HTMLFormElement>): Promise<void> { event.preventDefault(); setError(null); if (!email.trim() || !password) { setError('Enter your email and password to continue.'); return }; setSubmitting(true); try { await login(email.trim(), password); navigate(destination, { replace: true }) } catch (reason) { setError(reason instanceof Error ? reason.message : 'Unable to sign in right now.') } finally { setSubmitting(false) } }
  return <main className="login-canvas min-h-screen px-5 py-8 text-slate-100 sm:grid sm:place-items-center"><div className="grid w-full max-w-5xl overflow-hidden rounded-3xl border border-slate-700/70 bg-slate-950/80 shadow-2xl shadow-emerald-950/30 lg:grid-cols-[1.05fr_.95fr]"><section className="relative overflow-hidden border-b border-slate-800 p-8 sm:p-12 lg:border-b-0 lg:border-r"><div className="absolute -left-16 top-12 h-48 w-48 rounded-full bg-emerald-400/10 blur-3xl" /><div className="relative"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-emerald-400 font-bold text-slate-950">V</span><span className="font-semibold tracking-wide">Value Investing</span></div><p className="mt-16 text-xs font-semibold uppercase tracking-[.25em] text-emerald-300">A better starting point</p><h1 className="mt-4 max-w-md text-4xl font-semibold leading-tight text-white sm:text-5xl">Research before conviction.</h1><p className="mt-5 max-w-md leading-7 text-slate-300">A disciplined workspace for examining fundamentals, valuing businesses, and keeping your reasoning visible.</p><div className="mt-12 rounded-2xl border border-emerald-400/20 bg-slate-900/70 p-5"><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-300">The quiet advantage</p><p className="mt-3 text-sm leading-6 text-slate-300">Good investment decisions begin with patient questions, not louder opinions.</p></div></div></section><section className="p-8 sm:p-12"><p className="text-sm font-medium text-emerald-300">Welcome back</p><h2 className="mt-2 text-3xl font-semibold text-white">Enter your workspace</h2><p className="mt-2 text-sm text-slate-400">Sign in to continue your research.</p>{(message || error) && <div role="alert" className="mt-6 rounded-xl border border-amber-300/30 bg-amber-300/10 px-4 py-3 text-sm text-amber-100">{error ?? message}</div>}<form className="mt-8 space-y-5" onSubmit={submit}><label className="block text-sm font-medium text-slate-200">Email<input value={email} onChange={(event) => setEmail(event.target.value)} autoComplete="email" type="email" className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-900 px-4 py-3 text-white outline-none transition focus:border-emerald-400 focus:ring-2 focus:ring-emerald-400/30" placeholder="you@example.com" /></label><label className="block text-sm font-medium text-slate-200">Password<input value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="current-password" type="password" className="mt-2 w-full rounded-xl border border-slate-700 bg-slate-900 px-4 py-3 text-white outline-none transition focus:border-emerald-400 focus:ring-2 focus:ring-emerald-400/30" placeholder="Your password" /></label><button disabled={submitting} className="w-full rounded-xl bg-emerald-400 px-4 py-3 font-semibold text-slate-950 transition hover:bg-emerald-300 disabled:cursor-not-allowed disabled:opacity-60">{submitting ? 'Signing in…' : 'Sign in to research'}</button></form><p className="mt-8 text-xs leading-5 text-slate-500">Value Investing is decision support, not investment advice. Your judgment stays in the room.</p></section></div></main>
}
