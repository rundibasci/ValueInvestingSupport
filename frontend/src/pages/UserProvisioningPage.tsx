import { useState } from 'react'
import type { FormEvent } from 'react'
import { Navigate } from 'react-router-dom'
import { apiFetch } from '../api/client'
import { useAuth } from '../auth/AuthProvider'
export function UserProvisioningPage(): JSX.Element { const { session } = useAuth(); const [notice, setNotice] = useState<string | null>(null); if (session?.role !== 'ADMIN') return <Navigate to="/" replace />; async function submit(event: FormEvent<HTMLFormElement>): Promise<void> { event.preventDefault(); setNotice(null); const form = new FormData(event.currentTarget); const response = await apiFetch('/api/v1/admin/users', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: form.get('email'), password: form.get('password'), role: form.get('role') }) }); setNotice(response.ok ? 'User created successfully.' : response.status === 409 ? 'That email is already registered.' : 'Unable to create the user.'); if (response.ok) event.currentTarget.reset() }; return <section className="max-w-xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-emerald-400">Administration</p><h1 className="mt-2 text-3xl font-semibold text-white">Provision a user</h1><p className="mt-3 text-slate-400">Create an account for a colleague. This is not public registration.</p>{notice && <p role="status" className="mt-6 rounded-xl border border-slate-700 bg-slate-900 px-4 py-3 text-sm text-slate-200">{notice}</p>}<form className="mt-7 space-y-5 rounded-2xl border border-slate-800 bg-slate-900/50 p-6" onSubmit={submit}><label className="block text-sm">Email<input required name="email" type="email" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="block text-sm">Temporary password<input required minLength={8} name="password" type="password" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2" /></label><label className="block text-sm">Role<select name="role" defaultValue="INVESTOR" className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2"><option>INVESTOR</option><option>ADVISOR</option><option>ADMIN</option></select></label><button className="rounded-lg bg-emerald-400 px-4 py-2 font-semibold text-slate-950">Create user</button></form></section> }
