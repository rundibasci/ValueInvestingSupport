package it.mazzoni.vis.thesis;

/** Mirrors vis-model-training/schemas/thesis-output.schema.json -> properties.classification. */
public enum ThesisClassification {
    POTENTIALLY_UNDERVALUED, FAIRLY_VALUED, POTENTIALLY_OVERVALUED, UNDER_REVIEW, INSUFFICIENT_DATA
}
